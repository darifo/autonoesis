# Application

Commands, queries, use-case orchestration, transaction boundaries, and ports. Application code coordinates domain behavior without depending on concrete databases, queues, workflows, or model providers.
