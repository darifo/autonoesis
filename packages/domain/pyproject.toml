[project]
name = "autonoesis-domain"
version = "0.1.0"
description = "Pure domain model and invariants for Autonoesis"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/autonoesis_domain"]
