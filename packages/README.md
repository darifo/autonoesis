# Internal packages

Packages are independently testable modules but are not independently deployable services. Public behavior is exposed through explicit ports and contracts, never shared database tables.
