[project]
name = "autonoesis-contracts"
version = "0.1.0"
description = "Stable transport contracts for Autonoesis"
requires-python = ">=3.12"
dependencies = ["autonoesis-domain"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/autonoesis_contracts"]

[tool.uv.sources]
autonoesis-domain = { workspace = true }
