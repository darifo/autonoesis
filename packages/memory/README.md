# Memory

Memory provider ports, provenance, confidence, scope, TTL, conflict handling, deletion propagation, and stable-memory write gates. Honcho is one possible adapter, not the authority for business state.
